# puppeteer is an optional, best-quality PDF renderer. Skip the ~150MB Chromium
# download on install so `npm install` stays fast — report generation falls back
# to the built-in pdfkit renderer automatically when Chromium isn't present.
puppeteer_skip_download=true
